package com.pulse.music.core.designsystem

import androidx.compose.ui.graphics.Color

// Midnight Symphony Palette - Refined for "Neon Noir"

// Brand Colors
val PULSEPrimary = Color(0xFF7F5AF0) // Electric Indigo
val PULSEPrimaryDark = Color(0xFF5839C9)
val PULSEAccent = Color(0xFF2CB67D) // Cyber Green for success/accents
val PULSEHighlight = Color(0xFF00F0FF) // Neon Cyan

// Neutral Colors (OLED Optimized)
val PULSEBlack = Color(0xFF000000) // True Black
val PULSEDarkGray = Color(0xFF0D0D0E) // Rich Dark
val PULSESurface = Color(0xFF16161A) // Slightly lighter for cards
val PULSESurfaceVariant = Color(0xFF242629) // Input fields

// Text Colors
val PULSETextPrimary = Color(0xFFFFFFFE) // Off-white for less eye strain
val PULSETextSecondary = Color(0xFF94A1B2) // Cool gray
val PULSETextDisabled = Color(0xFF4A5568)

// Status Colors
val PULSEError = Color(0xFFFF5252)
val PULSESuccess = Color(0xFF2CB67D)

