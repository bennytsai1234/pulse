## 1. Foundation
- [x] 1.1 Deduplicate `MediaItem` mappers into `player/mapper` package.
- [ ] 1.2 Refactor `PulseAudioService` error handling to emit events.

## 2. Advanced Playback
- [ ] 2.1 Implement `GaplessPlaybackManager` to coordinate with `DualPlayerManager`.
- [ ] 2.2 Add `ReplayGain` support in `ExoPlayer` config.

## 3. Resilience
- [ ] 3.1 Add network recovery strategy for streaming.
- [ ] 3.2 Add corrupted file blacklist mechanism.
